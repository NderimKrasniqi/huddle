// Placeholder: the Game Module interface (metadata, settings schema,
// initial-state factory, reduce(state, event), TV/Controller screens) lands here
// in Phase 3. See docs/implementation-plan.md.
export {};
