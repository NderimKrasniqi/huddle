# Design QA

Current visual rules live only in
[`../soft-minimal-handoff.md`](../soft-minimal-handoff.md). Approved reference
boards remain under `../reference/`; they are inputs, not runtime assets.

This directory owns evidence only for the current platform-consolidation
milestone. Put new dated checklists, captures, and comparison output under
`evidence/platform-consolidation/`. Delete superseded proof instead of keeping
an in-repository archive; Git history remains the historical record.
