# Huddle Approved Screen Manifest

This folder records the approved Soft Minimal screen set used for the Expo phone and TV apps.

## Phone app

1. `phone-screens/01-join-room.png` — original standalone export.
2. `phone-screens/02-your-room-host-reference-crop.png` — exact crop from the approved final screen board. A separate full-resolution standalone export was not present in the original generated batch.
3. `phone-screens/03-manage-player-host.png` — original standalone export.
4. `phone-screens/04-pick-a-game-host.png` — original standalone export.
5. `phone-screens/05-waiting-player.png` — original standalone export.

The earlier “You’re in (player)” screen is intentionally not part of this final set because it was removed from the approved phone flow before the final board.

## TV app

1. `tv-screens/01-room.png` — original standalone Room screen export, combining pairing/join information with the current room roster and 12-seat layout.
2. `tv-screens/02-game-carousel.png` — original standalone Game carousel export.

## Source reference

`screens-reference/approved-soft-minimal-screen-board.png` is the final approved overview used to verify the screen set and visual relationships.
