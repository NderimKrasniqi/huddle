{
  "expo": {
    "name": "Huddle",
    "slug": "huddle-tv",
    "scheme": "huddle",
    "version": "0.1.0",
    "orientation": "landscape",
    "userInterfaceStyle": "light",
    "//backgroundColor": "Boardwalk's `colors.screen`, the token tv-stage.tsx paints. Stated here so the native window is already the right cream before any React view exists — otherwise a cold start flashes the platform default while the fonts resolve and TvLayout returns null. Kept honest by color-literals.test.ts, which is also why the value is written out: Expo transpiles app.config.ts and then requires it, so importing the token is not possible.",
    "backgroundColor": "#F7F1E6",
    "newArchEnabled": true,
    "android": {
      "package": "tv.huddle.hub"
    },
    "ios": {
      "bundleIdentifier": "tv.huddle.hub",
      "//infoPlist": "CALayer.allowsEdgeAntialiasing is false by default, and a layer carrying a transform is composited without antialiasing — so Boardwalk's ±1–2° sticker tilts turn every tile border into a visible staircase (measured: a rotated edge steps 2px per 50 rows with no intermediate pixel values, where Android feathers the same edge). This key turns edge antialiasing on app-wide. Apple documents a rendering cost; the TV screens are near-static, so it does not matter here. Only the TV app needs it — nothing in the Controller is rotated.",
      "infoPlist": {
        "UIViewEdgeAntialiasing": true
      }
    },
    "plugins": [
      "expo-router",
      ["@react-native-tvos/config-tv", { "isTV": true }]
    ],
    "experiments": {
      "typedRoutes": true
    }
  }
}
